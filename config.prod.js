// Optional: set window.__API_BASE__ before this file runs (e.g. CI build).
// Empty string = index.html will use localhost only on localhost/127.0.0.1, not on GitHub Pages.
window.__API_BASE__ = window.__API_BASE__ || ""
